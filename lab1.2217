print("[โปรแกรมหาพื้นที่สามเหลี่ยม]");
base = int(input("รับค่าฐาน: ")); hight = int(input("รับค่าสูง: "));
area = 1/2 * base * hight
print("--------------------------\n\nคำตอบ" + str(area)); print("--------------------------\n\n")